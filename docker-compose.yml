# Este archivo redirige al docker-compose.yml real en la carpeta backend
# Coolify está configurado para buscar en la raíz, pero nuestro archivo está en /backend/

version: '3.8'

# Importar el docker-compose real desde la carpeta backend  
include:
  - path: ./backend/docker-compose.yml
