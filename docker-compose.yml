# Archivo principal de docker-compose para Coolify
# Redirige al servicio real definido en backend/docker-compose.yml

version: '3.8'

services:
  # Servicio principal - referencia al servicio en backend
  studyscribe-backend-v2:
    extends:
      file: ./backend/docker-compose.yml
      service: studyscribe-backend-v2
